# ARCH_08_DEBATE_JUDGE - Pydantic AI + pydantic-graph

`GraphBuilder` crea un fork tipado para tres `DebateProposal`, un `Join`, un
paso `DebateRoundOutput` y un paso final `JudgeDecision`. Todas las estructuras
intermedias se validan con Pydantic y no existe ningun bucle.
